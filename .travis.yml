before_install:
  - gem install cocoapods -v '0.33.1'
  
language: objective-c
script:
  - xctool -workspace BeerChooser.xcworkspace -scheme BeerChooser -sdk iphonesimulator ONLY_ACTIVE_ARCH=NO build test